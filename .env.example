# Base URL of the backend API (see ../restaurant-erp/README.md to run it)
VITE_API_BASE_URL=http://localhost:4000

# Must match a tenant's code in the backend - the seed script creates "demo"
VITE_DEFAULT_TENANT_CODE=demo
